const age = 21;

if (age >= 18) {
    console.log("You can enter");
} else {
    console.log("You are underaged");
}

/* Conditional Logic. age is declared to be 21.
 If/Else is used in Conditional logic.
 If the age is greater or equals to 18, then console.log permits entry by stating intent.
 But, which is the use of else, console.log denies entry by stating underaged.

 When declaring a code that you won't be reassigning, use "const"

 Always use uniform Identation when writing codes. */
